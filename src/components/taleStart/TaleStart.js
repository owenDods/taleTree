import React from 'react';
// import PropTypes from 'prop-types';

export const className = 'taleStart';

const TaleStart = () => (<div className={className} />);

TaleStart.propTypes = {};

export default TaleStart;
